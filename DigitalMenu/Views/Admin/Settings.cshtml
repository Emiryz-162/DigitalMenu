@model DigitalMenu.ViewModels.SettingsViewModel
@{
    ViewData["Title"] = "Genel Ayarlar";
}

<!-- Dark Admin Navbar -->
<nav class="admin-navbar-dark">
    <div class="container-fluid px-3">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
            <h3 class="text-white mb-0 fs-5">
                <i class="bi bi-gear-fill me-2"></i>
                <span class="d-none d-md-inline">Genel Ayarlar</span>
                <span class="d-inline d-md-none">Ayarlar</span>
            </h3>
            <div class="d-flex gap-2">
                <a asp-action="Index" class="btn btn-light-dark btn-sm">
                    <i class="bi bi-house-fill me-1"></i>
                    <span class="d-none d-sm-inline">Ana Sayfa</span>
                </a>
                <a asp-controller="Account" asp-action="Logout" class="btn btn-light-dark btn-sm">
                    <i class="bi bi-box-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
</nav>

<div class="admin-container-dark">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success-dark alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle-fill me-2"></i>@TempData["SuccessMessage"]
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
                </div>
            }

            <div class="admin-card-dark">
                <h4 class="text-white mb-4 fs-5">
                    <i class="bi bi-gear me-2"></i>Restoran Bilgileri
                </h4>

                <form asp-action="Settings" method="post" enctype="multipart/form-data">
                    <input type="hidden" asp-for="Id" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger-dark"></div>

                    <div class="mb-4">
                        <label asp-for="RestaurantName" class="form-label-dark">
                            <i class="bi bi-shop me-1"></i>Restoran Adı <span class="text-danger">*</span>
                        </label>
                        <input asp-for="RestaurantName" class="form-control-dark" placeholder="Restoran adınızı girin" />
                        <span asp-validation-for="RestaurantName" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="WelcomeText" class="form-label-dark">
                            <i class="bi bi-chat-quote me-1"></i>Hoş Geldiniz Mesajı
                        </label>
                        <textarea asp-for="WelcomeText" class="form-control-dark" rows="2" placeholder="Hoş geldiniz mesajınız (opsiyonel)"></textarea>
                        <span asp-validation-for="WelcomeText" class="text-danger small"></span>
                        <div class="form-text-dark">Ana sayfada logo altında gösterilir</div>
                    </div>

                    <!-- Ayırıcı -->
                    <div class="settings-divider"></div>

                    <!-- Logo -->
                    <div class="mb-3">
                        <label class="form-label-dark">
                            <i class="bi bi-image me-1"></i>Mevcut Logo
                        </label>
                        @if (!string.IsNullOrEmpty(Model.LogoPath))
                        {
                            <div class="text-center bg-preview-dark p-3 rounded">
                                <img src="@Model.LogoPath" alt="Logo" id="currentLogo"
                                     class="img-fluid rounded"
                                     style="max-height: 150px; background: rgba(255,255,255,0.1); padding: 10px;">
                            </div>
                        }
                        else
                        {
                            <div class="empty-state-dark py-4">
                                <i class="bi bi-image" style="font-size: 3rem;"></i>
                                <p class="mb-0 small">Henüz logo yüklenmemiş</p>
                            </div>
                        }
                    </div>

                    <div class="mb-4">
                        <label asp-for="LogoFile" class="form-label-dark">
                            <i class="bi bi-upload me-1"></i>Yeni Logo Yükle
                        </label>
                        <input asp-for="LogoFile" class="form-control-dark" type="file" accept="image/*" id="logoInput" />
                        <span asp-validation-for="LogoFile" class="text-danger small"></span>
                        <div class="form-text-dark">
                            <i class="bi bi-info-circle me-1"></i>Seçmezseniz mevcut logo korunur
                        </div>
                        <div class="form-text-dark">
                            <strong>Önerilen:</strong> PNG formatı, şeffaf arkaplan • <strong>Max:</strong> 5MB
                        </div>

                        <!-- Yeni Logo Önizleme -->
                        <div id="newLogoPreview" class="mt-3 text-center bg-preview-dark p-3 rounded" style="display: none;">
                            <p class="text-success small mb-2">
                                <i class="bi bi-check-circle me-1"></i>Yeni Logo
                            </p>
                            <img id="logoPreviewImg" src="" alt="Logo Önizleme"
                                 class="img-fluid rounded"
                                 style="max-height: 150px; background: rgba(255,255,255,0.1); padding: 10px;">
                            <button type="button" class="btn btn-sm btn-outline-danger mt-2" id="removeNewLogo">
                                <i class="bi bi-x-circle me-1"></i>Kaldır
                            </button>
                        </div>
                    </div>

                    <div class="alert-dark-info mb-4">
                        <i class="bi bi-lightbulb me-2"></i>
                        <strong>İpucu:</strong> Logo, ana sayfada ve login ekranında gösterilir. Şeffaf PNG formatı en iyi sonucu verir.
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary-dark btn-lg">
                            <i class="bi bi-save me-2"></i>Ayarları Kaydet
                        </button>
                        <a asp-action="Index" class="btn btn-secondary-dark">
                            <i class="bi bi-x-circle me-2"></i>İptal
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        const logoInput = document.getElementById('logoInput');
        const currentLogo = document.getElementById('currentLogo');
        const newLogoPreview = document.getElementById('newLogoPreview');
        const logoPreviewImg = document.getElementById('logoPreviewImg');
        const removeLogoBtn = document.getElementById('removeNewLogo');

        // Logo önizleme
        if (logoInput) {
            logoInput.addEventListener('change', function(e) {
                const file = e.target.files[0];

                if (file) {
                    // Dosya boyutu kontrolü (5MB)
                    if (file.size > 5 * 1024 * 1024) {
                        alert('Dosya boyutu 5MB\'dan küçük olmalıdır!');
                        e.target.value = '';
                        newLogoPreview.style.display = 'none';
                        return;
                    }

                    // Dosya tipi kontrolü
                    const validTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/webp'];
                    if (!validTypes.includes(file.type)) {
                        alert('Sadece JPG, PNG ve WEBP formatları desteklenir!');
                        e.target.value = '';
                        newLogoPreview.style.display = 'none';
                        return;
                    }

                    // Önizleme göster
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        logoPreviewImg.src = e.target.result;
                        newLogoPreview.style.display = 'block';

                        // Mevcut logoyu soluklaştır
                        if (currentLogo) {
                            currentLogo.style.opacity = '0.4';
                        }

                        newLogoPreview.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                    }
                    reader.readAsDataURL(file);
                } else {
                    newLogoPreview.style.display = 'none';
                    if (currentLogo) {
                        currentLogo.style.opacity = '1';
                    }
                }
            });
        }

        // Logo kaldır butonu
        if (removeLogoBtn) {
            removeLogoBtn.addEventListener('click', function() {
                logoInput.value = '';
                newLogoPreview.style.display = 'none';
                if (currentLogo) {
                    currentLogo.style.opacity = '1';
                }
            });
        }
    </script>
}