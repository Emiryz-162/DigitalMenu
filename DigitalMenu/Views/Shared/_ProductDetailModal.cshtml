<!-- Ürün Detay Modal - Dark Theme -->
<div class="modal fade" id="productDetailModal" tabindex="-1" aria-labelledby="productDetailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-product-dark">
        <div class="modal-content modal-content-dark">
            <!-- Close Button -->
            <button type="button" class="btn-close-dark" data-bs-dismiss="modal" aria-label="Close">
                <i class="bi bi-x-lg"></i>
            </button>

            <!-- Ürün Görseli + Başlık Overlay -->
            <div class="modal-product-image">
                <img id="modalProductImage" src="" alt="" class="product-modal-img">
                <div class="modal-image-overlay">
                    <h3 id="modalProductName" class="modal-product-title"></h3>
                </div>
            </div>

            <!-- Ürün Bilgileri -->
            <div class="modal-product-body">
                <!-- Fiyat ve Kalori (Kutusuz) -->
                <div class="modal-product-info">
                    <div class="modal-price-simple">
                        <i class="bi bi-currency-dollar"></i>
                        <span id="modalProductPrice"></span>
                    </div>
                    <div id="modalProductCaloriesSection" class="modal-calories-simple" style="display: none;">
                        <i class="bi bi-fire"></i>
                        <span id="modalProductCalories"></span>
                    </div>
                </div>

                <!-- Ayırıcı Çizgi -->
                <div class="modal-divider"></div>

                <!-- Açıklama (Kutusuz) -->
                <div class="modal-description-simple">
                    <p id="modalProductDescription"></p>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function openProductModal(element) {
        // Data attributelerinden bilgileri al
        const name = element.getAttribute('data-product-name');
        const price = element.getAttribute('data-product-price');
        const description = element.getAttribute('data-product-description');
        const calories = element.getAttribute('data-product-calories');
        const imagePath = element.getAttribute('data-product-image');

        // Modal elemanlarını doldur
        document.getElementById('modalProductImage').src = imagePath;
        document.getElementById('modalProductImage').alt = name;
        document.getElementById('modalProductName').textContent = name;
        document.getElementById('modalProductPrice').textContent = price;
        document.getElementById('modalProductDescription').textContent = description;

        // Kalori bilgisi varsa göster (0'dan büyükse)
        const caloriesSection = document.getElementById('modalProductCaloriesSection');
        const caloriesText = document.getElementById('modalProductCalories');

        if (calories && calories !== '' && parseInt(calories) > 0) {
            caloriesText.textContent = calories + ' kcal';
            caloriesSection.style.display = 'flex';
        } else {
            caloriesSection.style.display = 'none';
        }

        // Modal'ı aç
        const modal = new bootstrap.Modal(document.getElementById('productDetailModal'));
        modal.show();
    }
</script>